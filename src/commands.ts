/**
 * commands.ts — Office ribbon command handlers (minimal stub).
 */

Office.onReady(() => {
  // Ribbon button commands can be defined here if needed.
  // Currently we only use the ShowTaskpane action in manifest.
});
